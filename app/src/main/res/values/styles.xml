<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.SplashScreen">
        <!-- Full-screen background -->
        <item name="windowSplashScreenBackground">@color/background</item>
        <!-- JavaZone icon in the circular centre slot, inset so the full image clears the circular clip -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_center_icon</item>
        <!-- Match the icon background circle to the splash background -->
        <item name="windowSplashScreenIconBackgroundColor">@color/background</item>
        <!-- Ocean image shown uncropped at the bottom (API 31+, minSdk 36 so always active) -->
        <item name="android:windowSplashScreenBrandingImage">@drawable/splash_branding</item>
        <!-- Theme to apply once the splash screen dismisses -->
        <item name="postSplashScreenTheme">@style/AppTheme</item>
    </style>
</resources>
